{
  "name": "arpg-web",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "vendor": "node scripts/vendor-foundations.mjs",
    "wasm": "wasm-pack build ../crates/arpg-web-wasm --target web --out-dir ../../web/src/wasm --out-name arpg_web_wasm -- --locked",
    "test": "bun test src/virtual-stick.test.js src/dedicated-session.test.js src/jsx-runtime.test.js src/character-selection.test.js",
    "build": "bun run vendor && bun run wasm && vite build",
    "dev": "bun run vendor && bun run wasm && vite"
  },
  "dependencies": {
    "@moritzbrantner/three-d-renderer": "github:moritzbrantner/3d-lab#7889a0066df928b101d42fb16f32353bd26c0696",
    "react": "19.1.1",
    "react-dom": "19.1.1",
    "three": "0.185.1"
  },
  "devDependencies": {
    "vite": "8.3.1"
  }
}
