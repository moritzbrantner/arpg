{
  "name": "arpg-web",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "vendor": "node scripts/vendor-foundations.mjs",
    "wasm": "wasm-pack build ../crates/arpg-web-wasm --target web --out-dir ../../web/src/wasm --out-name arpg_web_wasm -- --locked",
    "test": "bun test src/virtual-stick.test.js src/dedicated-session.test.js",
    "test:wasm": "wasm-pack build ../crates/arpg-web-wasm --target nodejs --out-dir ../../web/.wasm-test --out-name arpg_web_wasm -- --locked && node scripts/wasm-contract.mjs && rm -rf .wasm-test",
    "test:e2e": "playwright test",
    "build": "bun run vendor && bun run wasm && vite build",
    "dev": "bun run vendor && bun run wasm && vite"
  },
  "dependencies": {
    "@moritzbrantner/three-d-renderer": "github:moritzbrantner/3d-lab#2af3ef54bb515c8f5611d4f7d39d484202a42784",
    "react": "19.1.1",
    "react-dom": "19.1.1",
    "three": "0.185.1"
  },
  "devDependencies": {
    "vite": "7.1.5"
  }
}
